# ── Core ─────────────────────────────────────────────────
streamlit>=1.35.0
pandas>=2.0.0
numpy>=1.24.0
plotly>=5.18.0
scipy>=1.11.0
statsmodels>=0.14.0
scikit-learn>=1.3.0

# ── API layer ─────────────────────────────────────────────
fastapi>=0.110.0
uvicorn>=0.27.0
python-multipart>=0.0.9
python-jose[cryptography]>=3.3.0

# ── LLM providers ─────────────────────────────────────────
openai>=1.30.0
anthropic>=0.25.0

# ── Vector memory (v0.6) ──────────────────────────────────
chromadb>=0.5.0
sentence-transformers>=2.7.0   # local embeddings (internet_off_mode)
# pgvector is provided by the PostgreSQL extension — no pip package needed

# ── Data connectors (v0.6) ────────────────────────────────
pyathena>=3.0.0
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0         # PostgreSQL + pgvector + Redshift
snowflake-sqlalchemy>=1.5.0    # Snowflake
snowflake-connector-python>=3.0.0
google-cloud-bigquery>=3.11.0  # BigQuery
google-auth>=2.23.0
db-dtypes>=1.2.0               # BigQuery pandas dtypes

# ── Scheduler (v0.6) ──────────────────────────────────────
apscheduler>=3.10.0

# ── dbt adapter (v0.6) ────────────────────────────────────
pyyaml>=6.0.0                  # already needed for configs

# ── Ingestion ─────────────────────────────────────────────
pdfplumber>=0.9.0
pypdf>=3.0.0
python-docx>=1.0.0
pytesseract>=0.3.10
Pillow>=10.0.0
openpyxl>=3.1.0

# ── Forecasting ───────────────────────────────────────────
prophet>=1.1.0

# ── Config + utilities ────────────────────────────────────
python-dotenv>=1.0.0
requests>=2.31.0

# ── Local LLM (optional — for internet_off_mode) ──────────
# ollama>=0.1.0

# ── Streaming data (optional — future) ───────────────────
# kafka-python>=2.0.0
