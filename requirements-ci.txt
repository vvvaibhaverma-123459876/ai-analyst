# requirements-ci.txt
# Lean install for GitHub Actions CI.
# Heavy optional deps (prophet, chromadb, sentence-transformers,
# snowflake, bigquery, redshift, streamlit) are intentionally excluded:
#   - prophet / chromadb / sentence-transformers: slow install (5-10 min),
#     not needed because their call sites are guarded with try/except ImportError
#   - snowflake / bigquery / redshift: require live credentials; all
#     connector tests use monkeypatch to skip connection probing
#   - streamlit: only in app/ui/ which is not tested in the benchmark suite

# ── Core analysis ────────────────────────────────────────
pandas>=2.0.0
numpy>=1.24.0
scipy>=1.11.0
scikit-learn>=1.3.0
statsmodels>=0.14.0

# ── Testing ───────────────────────────────────────────────
pytest>=8.0.0
pytest-cov>=5.0.0
pytest-timeout>=2.3.0
pytest-xdist>=3.5.0         # parallel -n auto

# ── API + security ────────────────────────────────────────
fastapi>=0.110.0
python-jose[cryptography]>=3.3.0
python-dotenv>=1.0.0

# ── Data connectors (lightweight only) ───────────────────
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0      # used by postgres + redshift connectors
pyathena>=3.0.0

# ── Ingestion ─────────────────────────────────────────────
pyyaml>=6.0.0
openpyxl>=3.1.0
Pillow>=10.0.0
pypdf>=3.0.0
python-docx>=1.0.0

# ── Scheduler ─────────────────────────────────────────────
apscheduler>=3.10.0

# ── Misc utilities ────────────────────────────────────────
requests>=2.31.0
